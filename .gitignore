# LaTeX build artifacts (intermediates live in .build/, see Makefile)
*.aux
*.log
*.out
*.synctex.gz
.build/

# Site build output (regenerated by CI on every push)
dist/

# Local editor / OS noise
.claude/
.DS_Store
